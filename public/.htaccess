RewriteEngine On
RewriteBase /public/

# Prevent access to hidden files
RewriteRule "^\." - [F]

# If not a file or directory, route to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?page=$1 [QSA,L]
